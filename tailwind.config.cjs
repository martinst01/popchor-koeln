const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			sans: ['Open Sans'],
		},
		extend: {
			screens: {
				xs: '480px',
			},
		},
	},

	plugins: [],
};

module.exports = config;
